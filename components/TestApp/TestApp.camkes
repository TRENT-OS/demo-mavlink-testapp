#include <if_OS_Socket.camkes>

component TestApp {
    control;

    // Networking
    IF_OS_SOCKET_USE(networkStack)

    // Flight controller
    IF_FCADAPTER_USE(fcAdapter)

    consumes PingReady pingready;
    emits PingRequested pingreq;
}
